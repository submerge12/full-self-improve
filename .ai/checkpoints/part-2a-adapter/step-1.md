## Step 1

What I did: Added SourceAdapter conformance tests and MarkdownVaultAdapter tests, then confirmed they failed before implementation because the adapter modules were missing.
Files modified: [src/engine/source-adapter.test.ts, src/adapters/markdown-vault.test.ts]
Test status: failing as expected before implementation
Next step: Implement the SourceAdapter types and MarkdownVaultAdapter.
