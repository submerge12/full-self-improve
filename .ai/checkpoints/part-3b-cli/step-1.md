## Step 1

What I did: Added CLI handler tests and confirmed RED states: missing CLI module first, then TypeScript/runtime failures before the runner and type narrowing fixes.
Files modified: [src/cli/kl.test.ts, package.json]
Test status: failing as expected before implementation/fixes
Next step: Implement CLI handler and runtime script support.
