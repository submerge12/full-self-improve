## Step 2

What I did: Wired kl plan --db to open SQLite, apply migrations, call createPersistentDailyPlan, close the database, and return a mock-persistent JSON result while preserving mock --concept mode.
Files modified: [G:/knowledge-loop/src/cli/kl.ts, G:/knowledge-loop/src/cli/kl.test.ts]
Test status: passing
Next step: Run npm run check and record the final verification result.
