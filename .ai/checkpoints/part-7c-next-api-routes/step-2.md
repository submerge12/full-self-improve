# Part 7C Step 2

- Added `npm run dev` as `next dev` while preserving existing scripts.
- Added the shared Web `Request` adapter and env-backed runtime context factory.
- Added thin App Router route modules only; business behavior still goes through `handleApiRequest`.
