## Step 1

What I did: Added the persistent daily-plan application queue signal with TDD coverage for due review/application pairs, stored queue parsing, illegal type rejection, future-review exclusion, and force regeneration.
Files modified: [src/engine/mock-commands.ts, src/engine/persistent-plan.ts, src/engine/persistent-plan.test.ts]
Test status: passing
Next step: Run final requested verification commands and report results.
