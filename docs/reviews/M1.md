# M1 Review - knowledge-loop Standalone

Date: 2026-06-13

Status: live demo verified, not fully closed. M1 now has one-sitting standalone evidence for Holly ingest, daily plan, quiz, teach-back, mastery change, and public/private wiki rendering. Strict milestone closure remains blocked by the section 0 frozen-repo working-tree criterion.

## Verified

- Project verification passes in mock/no-key mode.
  - Evidence: `.ai/checkpoints/part-2-1-no-api-key-check/step-1.md`.
  - Command: clear `LLM_PROVIDER`, `DEEPSEEK_API_KEY`, `QWEN_API_KEY`, and `OPENAI_API_KEY`, then run `npm run check`.
  - Result: typecheck passed, lint passed, Vitest 23 files / 272 tests passed.
- Private UI and public wiki render from a runtime SQLite DB.
  - Evidence: `.ai/checkpoints/part-2-1-dev-render-smoke/step-3.md`.
  - Result: `npx next build --webpack` passed; `/`, `/learn`, `/wiki`, `/wiki/1`, and private `/wiki/2` 404 smoke checks passed on `next dev --webpack`.
- Holly dataset runtime ingest proof is recorded.
  - Evidence: `.ai/checkpoints/part-2-2-holly-real-ingest-proof/step-1.md` through `step-3.md`.
  - Result: raw vault scan found 535 markdown files, 10 default-excluded files, and 525 kept files; runtime `holly-vault` listed 525; scratch DB persisted 525 `sources`, 4408 `chunks`, 2141 `concepts`, 2141 `pages`, and 15366 `trace_events`; excluded persisted `doc_ref` count was 0.
- Full mock-persistent learning flow is covered through the CLI.
  - Evidence: `.ai/checkpoints/part-6m-mock-full-flow/step-1.md` through `step-3.md`.
  - Result: no-key flow runs `ingest`, `plan`, `quiz`, `teachback`, `diagnose`, and `trace` against one SQLite DB; focused CLI test passed.
- API surface has code-generated route documentation and bearer-token smoke coverage.
  - Evidence: `.ai/checkpoints/part-7e-api-doc-generation/step-3.md` and `.ai/checkpoints/part-2-5-api-external-bearer-smoke/step-3.md`.
  - Result: route manifest docs are generated from code; protected App Route exports are covered with bearer-authenticated requests; unauthenticated protected mutations are rejected before malformed body parsing.
- Public wiki privacy and detail behavior is covered.
  - Evidence: `.ai/checkpoints/part-8a-next-pages/step-3.md`, `.ai/checkpoints/part-8b-public-wiki-detail/step-3.md`, and `.ai/checkpoints/part-2-1-dev-render-smoke/step-3.md`.
  - Result: public listing excludes private pages; detail route returns public page data with provenance; private page detail returns 404/null in tests and smoke checks.
- M1 one-sitting live demo is covered against a fresh scratch DB.
  - Evidence: `.ai/checkpoints/part-m1-live-demo/step-1.md` and `.ai/checkpoints/part-m1-live-demo/step-2.md`.
  - Result: runtime `holly-vault` ingest processed 525 sources into 4408 chunks, 2141 concepts, and 2141 pages; `/api/plan/today` returned a 6423-item plan for 2026-06-13; a quiz and teach-back for `moonshot-ai` both succeeded; mastery changed from no row to score `0.22`; the learning page rendered; the public wiki listed the promoted public page; `/wiki/861` rendered provenance; the private control page `/wiki/1` returned 404 and did not expose its title.

## Deferred

- M1 is not marked complete because section 0 still requires frozen repo working trees to be clean or explicitly baselined, and current external checkout state does not satisfy that criterion.
- The real Holly ingest run had zero source failures, so real-dataset failure isolation did not trigger; failure behavior is covered by unit/integration tests and trace contracts.
- Section 3 orchestration is out of scope for this M1 review and is not counted as M1 evidence.

## Checklist Reconciliation

### Section 0 Ground Rules

| Criterion | Status | Evidence |
| --- | --- | --- |
| Frozen repos untouched by this project | Blocked by current external checkout state | `.ai/checkpoints/part-0-frozen-repo-status-audit/step-1.md`; `.ai/checkpoints/part-m1-live-demo/step-3.md`. Current statuses are dirty or not-git from the configured paths; no frozen repo edits were made for this live demo. |
| No imports/writes into frozen repo directories | Verified for source boundary | `src/project-boundary.test.ts` is part of `npm run check`; source-boundary tests passed in the latest review verification. |
| No API key mock mode completes end-to-end | Verified for CLI flow | `.ai/checkpoints/part-6m-mock-full-flow/step-1.md` through `step-3.md`; `.ai/checkpoints/part-2-1-no-api-key-check/step-1.md`. |
| Every pipeline step writes trace events | Verified by unit/integration coverage | Trace persistence and CLI trace coverage are recorded in `.ai/checkpoints/part-6j-persistent-trace-store/`, `part-6k-cli-persistent-trace/`, `part-6l-cli-persist-traces/`, and `part-6m-mock-full-flow/`. |

### Section 2.1 Stack

| Criterion | Status | Evidence |
| --- | --- | --- |
| `npm run check` passes | Verified | Latest review verification: 23 files / 272 tests passed. |
| `npm run dev` starts; private UI and public wiki render | Verified by smoke | `.ai/checkpoints/part-2-1-dev-render-smoke/step-3.md`. |
| `src/engine/` has zero Next/app imports | Verified | `.ai/checkpoints/part-2-1-engine-boundary/`; included in `npm run check`. |
| CLI commands run headless against same DB | Verified for implemented CLI flow | `.ai/checkpoints/part-6m-mock-full-flow/step-1.md` through `step-3.md`. |
| Deleting API key env vars and full suite still passes | Verified | `.ai/checkpoints/part-2-1-no-api-key-check/step-1.md`. |
| Switching `LLM_PROVIDER` requires env changes only | Verified for health/provider matrix | `.ai/checkpoints/part-2-1-provider-env-matrix/`; `src/engine/health.test.ts`. |

### Section 2.2 Source Adapters

| Criterion | Status | Evidence |
| --- | --- | --- |
| Shared adapter conformance suite exists | Verified | `.ai/checkpoints/part-2a-adapter/`. |
| `MarkdownVaultAdapter` passes fixture conformance | Verified | `.ai/checkpoints/part-2a-adapter/`; `src/adapters/markdown-vault.test.ts`. |
| Holly config lists documents and excludes private globs from `sources` | Verified | `.ai/checkpoints/part-2-2-holly-real-ingest-proof/step-1.md` through `step-3.md`. |
| Editing one vault file changes only that fingerprint | Verified by adapter tests | `src/adapters/markdown-vault.test.ts`; covered by `npm run check`. |
| Second adapter genericity proof | Out of M1 scope | PLAN marks this as M5. |

### Section 2.3 Data Model

| Criterion | Status | Evidence |
| --- | --- | --- |
| Migrations create schema and are replayable | Verified | `.ai/checkpoints/part-4a-db-schema/`; `src/db/migrations.test.ts`. |
| Cycle-creating concept edge rejected and logged | Verified for rejection | `.ai/checkpoints/part-4b1-graph-store/`; graph-store tests. Logging evidence should be rechecked before declaring M1 complete. |
| Page citations resolve and public pages require citations | Verified | `.ai/checkpoints/part-4b2-content-store/`; public wiki tests. |
| `mastery` has a single engine writer | Verified | `.ai/checkpoints/part-4c-mastery-single-writer/`. |
| Schema changes after M1 ship as new migrations | Deferred | Process rule for future changes; not closed by this review. |

### Section 2.4 Pipelines

| Criterion | Status | Evidence |
| --- | --- | --- |
| Running `kl ingest` twice makes second run a no-op | Verified | `.ai/checkpoints/part-5c1-preflight-noop/`. |
| Editing one source reprocesses only affected content | Verified | `.ai/checkpoints/part-5c2-changed-source-reprocess/`. |
| Holly ingest completes without crashing; failures recorded per-source | Verified for current dataset completion; failure path covered by tests | `.ai/checkpoints/part-2-2-holly-real-ingest-proof/`; `.ai/checkpoints/part-5d-ingest-source-failure-isolation/`. |
| Mock ingestion yields concepts and pages | Verified | `.ai/checkpoints/part-5a-persistent-ingest/`; `.ai/checkpoints/part-6m-mock-full-flow/`. |
| Generated pages carry citations and rendered pages show provenance | Verified | `.ai/checkpoints/part-8b-public-wiki-detail/step-3.md`; `.ai/checkpoints/part-2-1-dev-render-smoke/step-3.md`. |
| `kl plan` deterministic for same mastery/date | Verified | `.ai/checkpoints/part-6a-persistent-daily-plan/`; `.ai/checkpoints/part-6b-cli-persistent-plan/`. |
| Plan respects prerequisite order | Verified by planner tests | `.ai/checkpoints/part-6a-persistent-daily-plan/`; covered by `npm run check`. |
| Quiz grading updates mastery | Verified | `.ai/checkpoints/part-6c-persistent-exact-quiz/`; `.ai/checkpoints/part-6d-cli-persistent-quiz/`. |
| Teach-back returns score/gaps and updates mastery | Verified | `.ai/checkpoints/part-6f-persistent-teachback-engine/`; `.ai/checkpoints/part-6g-cli-persistent-teachback/`. |
| Diagnose feeds next plan after deliberate wrong answers | Verified by persistent diagnose/flow tests | `.ai/checkpoints/part-6h-persistent-diagnose-engine/`; `.ai/checkpoints/part-6i-cli-persistent-diagnose/`; `.ai/checkpoints/part-6m-mock-full-flow/`. |
| Every pipeline stage emits queryable trace events | Verified | `.ai/checkpoints/part-6j-persistent-trace-store/`, `part-6k-cli-persistent-trace/`, `part-6l-cli-persist-traces/`. |

### Section 2.5 API Surface

| Criterion | Status | Evidence |
| --- | --- | --- |
| Every endpoint has integration tests | Verified | `.ai/checkpoints/part-7b-api-handlers/`; `.ai/checkpoints/part-7c-next-api-routes/`; `.ai/checkpoints/part-7d-api-plan-generate/`. |
| `GET /api/plan/today` idempotent | Verified | API handler tests included in `npm run check`; checkpointed under `part-7b-api-handlers/`. |
| Public site/API cannot return private pages | Verified | `.ai/checkpoints/part-8a-next-pages/`; `.ai/checkpoints/part-8b-public-wiki-detail/`; `.ai/checkpoints/part-2-1-dev-render-smoke/step-3.md`. |
| API docs generated from code | Verified | `.ai/checkpoints/part-7e-api-doc-generation/step-3.md`. |
| Endpoints callable externally with bearer token; unauthenticated mutations rejected | Verified | `.ai/checkpoints/part-2-5-api-external-bearer-smoke/step-3.md`. |

## Verification For This Review Note

- Live M1 demo runner: passed; `POST /api/ingest/run?adapter=holly-vault`, `GET /api/plan/today`, `POST /api/quiz/grade`, `POST /api/teachback`, `GET /api/mastery/summary`, and wiki HTML checks all succeeded against `.ai/tmp/m1-live-demo/m1-live-demo-20260613-163938.db`.
- In-app browser render smoke: passed for `/learn`, `/wiki`, and `/wiki/861`; private control page title stayed hidden, with 404 status verified by the HTTP runner.
- `npm run check`: passed; typecheck and lint passed; Vitest 23 files / 272 tests passed.
- `npm audit --audit-level=moderate`: passed; found 0 vulnerabilities.
- `git diff --check`: passed.

## Remaining M1 Closure Gate

Record either clean `git status --short` output for every frozen git repo, or an explicit user-approved baseline for existing dirty/not-git external checkout state. Until that exists, M1 should be reported as live-demo verified but not strictly closed.
