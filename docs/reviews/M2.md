# M2 Review - Orchestration Spine

Date: 2026-06-14.

Status: offline/live-gate implementation evidence recorded; strict M2 closure pending required live proofs and global Section 0/frozen-repo baseline. This note satisfies the process-level review-note slice for M2 evidence, but it does not declare M2 complete.

## Verified Implementation Evidence

- Agent profiles and boundary config are in this repo.
  - Evidence: `src/agents/profiles.ts` defines Librarian, Scholar, and Nutritionist profiles with dry-run commands, write/destructive defaults denied, and network set to ask.
  - Evidence: `config/multica/board-publish.example.json`, `config/multica/live-smoke.example.json`, and `docs/runbooks/m2-multica.md` document Multica boundary wiring as external/self-hosted and pending live smoke.
  - Checkpoints: `.ai/checkpoints/part-m2-agent-profiles-config/step-1.md` and `step-2.md`.
- Dry-run board-day sequence exists for the M2 roles.
  - Evidence: `src/agents/dry-run.ts` plans the sequence Librarian nightly ingest, Scholar morning plan, Nutritionist daily meals, and Scholar evening mastery with `externalWrites: []`.
  - Evidence: `src/cli/kl.test.ts` covers `agent` and `agent-day` dry-run output, including intended Multica actions and no external writes.
  - Checkpoints: `.ai/checkpoints/part-m2-agent-dry-run/step-1.md` and `step-2.md`.
- Injectable executor and day runner exist for dry-run and gated live execution.
  - Evidence: `src/agents/executor.ts` and `src/agents/day-runner.ts` use injected read, board, and optional cost clients; blockers are board-visible actions, redacted, and later independent agents continue.
  - Evidence: `src/agents/day-runner.test.ts`, `src/agents/executor.test.ts`, and `src/agents/http-clients.test.ts` cover blocked, publish-failure, and redaction paths.
  - Checkpoints: `.ai/checkpoints/part-m2-agent-executor/step-1.md`, `.ai/checkpoints/part-m2-agent-day-runner/step-1.md` through `step-3.md`.
- HTTP read and Multica publish clients are implemented behind injectable interfaces.
  - Evidence: `src/agents/http-clients.ts` handles bearer headers, JSON/text reads, configured board publish endpoints, non-HTTP rejection, URL-credential rejection, and secret/path redaction.
  - Checkpoints: `.ai/checkpoints/part-m2-agent-http-clients/step-1.md` through `step-3.md`.
- Gated live manual trigger and scheduler paths exist, but they do not prove hands-free execution by themselves.
  - Evidence: `src/cli/kl.ts` exposes `agent-day --live` only with explicit `--live`, explicit Multica task/comment URLs, and runtime bearer tokens; `agent-schedule --dry-run` reports deterministic scheduler intent; `agent-schedule --live` is a single-shot trigger for an external scheduler.
  - Evidence: `docs/runbooks/m2-multica.md` states the live single-shot trigger does not install a daemon and does not make the M2 hands-free proof.
  - Checkpoints: `.ai/checkpoints/part-m2-agent-day-live-cli/step-1.md`, `.ai/checkpoints/part-m2-agent-schedule-dry-run/step-1.md` and `step-2.md`, `.ai/checkpoints/part-m2-schedule-live-trigger/step-1.md`.
- Live-smoke manifest, CLI preflight, board publish config validation, and board-day evidence validation are present as offline/post-observation validators.
  - Evidence: `config/multica/live-smoke.example.json` defines two consecutive evidence days and required board evidence for ingest report, study plan, meals, and evening mastery.
  - Evidence: `config/multica/board-day-evidence.example.json` is explicitly `observed_live_smoke_pending_verification` and says it does not prove hands-free execution or close M2.
  - Evidence: `src/cli/kl.ts` exposes `agent-live-smoke`, `agent-preflight`, `agent-board-config`, and `agent-board-evidence` as dry-run/offline validators; the preflight returns required live proofs as `not_verified_offline`.
  - Checkpoints: `.ai/checkpoints/part-m2-live-smoke-manifest/step-1.md` and `step-2.md`, `.ai/checkpoints/part-m2-agent-live-smoke-cli/step-1.md`, `.ai/checkpoints/part-m2-agent-preflight-report/step-1.md`, `.ai/checkpoints/part-m2-board-publish-validator/step-1.md`, `.ai/checkpoints/part-m2-preflight-board-config/step-1.md`, `.ai/checkpoints/part-m2-board-day-evidence/step-1.md`.
- Failure blocker smoke exists only as an offline simulation.
  - Evidence: `src/agents/failure-smoke.ts` and `src/cli/kl.test.ts` simulate a selected endpoint failure, publish a blocker action with redacted detail, and continue independent later agents.
  - Evidence: `docs/runbooks/m2-multica.md` states this does not kill a real API service, does not call Multica, does not prove live blocker visibility, and does not close M2.
  - Checkpoint: `.ai/checkpoints/part-m2-failure-blocker-smoke/step-1.md`.
- Evening mastery renderer is implemented for the live Scholar evening phase.
  - Evidence: `src/agents/mastery-report.ts` requires a `routeId: "mastery.summary"` API success body, renders rows, weak spots, top weak spot, diagnosis run id, date/source labels, and redacts dynamic values.
  - Evidence: `src/agents/executor.test.ts` covers publishing a live evening Scholar report, blocker behavior for malformed summaries, and ignoring lookalike endpoints.
  - Checkpoint: `.ai/checkpoints/part-m2-evening-mastery-report/step-1.md`.
- Daily cost visibility exists at report level.
  - Evidence: `src/agents/day-runner.ts` reports per-agent `llmCost`; dry-run remains `dry-run-no-llm`, live without a cost snapshot is explicit `not_configured`, and injected live snapshots use `pi-harness-live`.
  - Evidence: `docs/runbooks/m2-multica.md` states this wiring does not close the live cost proof until a real run captures non-placeholder cost data.
  - Checkpoint: `.ai/checkpoints/part-m2-daily-cost-visibility/step-1.md`.
- pi-harness preflight and runtime import proof paths exist, but the clean external dependency gate remains pending.
  - Evidence: `src/agents/pi-harness-dependency.ts` validates package metadata, dist entries, CLI bin, new-agent script shape, checkout cleanliness, and optional runtime import symbols through dry-run preflight.
  - Evidence: `.ai/checkpoints/part-m2-pi-harness-runtime-link/step-1.md` records a local no-save runtime import proof passing while the strict external clean-checkout blocker remains.
  - Checkpoints: `.ai/checkpoints/part-m2-pi-harness-dependency-preflight/step-1.md`, `.ai/checkpoints/part-m2-pi-harness-runtime-import/step-1.md`, `.ai/checkpoints/part-m2-pi-harness-runtime-link/step-1.md`.
- Multica self-host boundary has partial live runtime evidence, not row closure.
  - Evidence: `.ai/checkpoints/part-m2-multica-selfhost-live/step-1.md` records Docker/compose self-host runtime verification from `G:\multica-ai-multica-https-github-com`, with a strict clean-repo blocker still called out.

## Checklist Reconciliation

### PLAN Section 3 Orchestration Layer

| Criterion | Status | Evidence |
| --- | --- | --- |
| Multica runs locally from its unmodified repo; this repo contains only env/config | Partial live boundary evidence; strict external clean baseline pending | `docs/runbooks/m2-multica.md`; `.ai/checkpoints/part-m2-multica-selfhost-live/step-1.md`; `config/multica/*.json`. |
| pi-harness consumed as dependency and `G:\pi-harness` stays clean | Pending strict live/dependency proof | Preflight/runtime import tooling exists; `.ai/checkpoints/part-m2-pi-harness-runtime-link/step-1.md` says runtime import passed but clean-checkout blocker remains. |
| Agent profiles live in this repo and have dry-run mode without posting | Verified at implementation/test level | `src/agents/profiles.ts`; `src/agents/dry-run.ts`; profile and dry-run tests; related M2 checkpoints. |
| End-to-end day test shows ingest report, study plan, and meals without manual prompting | Pending live proof | Dry-run and gated live paths exist, but no captured two consecutive hands-free Multica board days are recorded. |
| Agent failures surface as board comments/blockers | Verified offline/test level; pending live kill-API proof | Executor/day-runner/failure-smoke tests and checkpoints; live board blocker capture remains pending. |
| Evening Scholar posts mastery delta matching `GET /api/mastery/summary` | Renderer verified at implementation/test level; pending live API comparison | `src/agents/mastery-report.ts`; executor tests; live board comment comparison remains pending. |
| Total daily LLM cost per agent is visible | Report wiring verified; pending live cost data | `src/agents/day-runner.ts`; daily-cost checkpoint; real pi-harness cost snapshot capture remains pending. |

### PLAN Section 5 M2 Row

| Done-when item | Status | Evidence |
| --- | --- | --- |
| One Multica board day shows ingest report produced by agents | Pending live proof | Offline manifest and validators define required evidence; no accepted live observed board-day proof is recorded. |
| One Multica board day shows study plan produced by agents | Pending live proof | Dry-run/gated live implementation exists; no strict live board proof is recorded. |
| One Multica board day shows meals from compass-health produced by agents | Pending live proof | Nutritionist reads meal plan and procurement endpoints in implementation/tests; no strict live board proof is recorded. |
| All items are produced hands-free, two days in a row | Pending live proof | `config/multica/live-smoke.example.json` defines two days, and board evidence validation exists, but this is not captured as verified hands-free Multica evidence. |

### Process-Level Review Note

This file records verified checklist items, how they were verified, and deferred items with reasons. It satisfies the documentation/evidence slice for M2 review-note creation only. It does not tick the M2 done-when row, and it does not re-verify Section 0 frozen-repo criteria as passed.

## Required Live Proofs

| Proof id | Status | Reason |
| --- | --- | --- |
| `multica_self_host_verified` | Pending live proof / `not_verified_offline` | A self-host runtime checkpoint exists, but strict closure still needs an accepted clean/baselined external Multica repo state and live board contract confirmation. |
| `pi_harness_dependency_clean` | Pending live proof / `not_verified_offline` | Runtime import tooling and a local no-save link proof exist, but the strict `G:\pi-harness` clean-checkout dependency proof remains blocked/pending. |
| `two_consecutive_hands_free_board_days` | Pending live proof / `not_verified_offline` | No captured evidence shows two consecutive Multica board days produced by scheduler-driven agents without manual prompting. |
| `failure_blocker_board_comment` | Pending live proof / `not_verified_offline` | Offline blocker simulation exists; the live drill killing knowledge-loop API and capturing the visible Multica blocker is not recorded. |
| `evening_mastery_delta_matches_api` | Pending live proof / `not_verified_offline` | Renderer and executor tests exist; no live board comment has been compared with the live `GET /api/mastery/summary` response. |
| `daily_cost_visible` | Pending live proof / `not_verified_offline` | Report fields exist; no live run with real pi-harness cost snapshot data is recorded. |

## Verification

- Controller final verification for this note:
  - Initial sandboxed `npm run check`: typecheck and lint completed, then Vitest failed while loading config with the known `spawn EPERM` sandbox error.
  - Escalated `npm run check`: passed on 2026-06-14; typecheck passed, lint passed, and Vitest reported 39 test files / 502 tests passed.
- Focused source review for this note:
  - Read `PLAN.md`, `docs/reviews/M1.md`, `docs/reviews/M3.md`, `docs/runbooks/m2-multica.md`, `src/cli/kl.ts`, `src/agents/*.ts`, `src/agents/*.test.ts`, `src/cli/kl.test.ts`, `config/multica/*.json`, and `.ai/checkpoints/part-m2-*/step-*.md`.
  - No npm tests were run by this worker for this documentation-only slice.
- Lightweight content verification for this note and its checkpoint is recorded in `.ai/checkpoints/part-m2-review-note/step-1.md`.

## Remaining Gates

- Strict M2 closure requires live Multica/scheduler evidence: two consecutive hands-free board days with ingest report, study plan, and meals produced by agents.
- Strict M2 closure requires clean or user-baselined external repo state for the frozen-repo criteria, including Multica and pi-harness.
- The pi-harness dependency must be proven clean and consumable in the live environment.
- The failure blocker path must be proven with a live board comment after killing or disabling knowledge-loop's API mid-run.
- The evening mastery board comment must be compared with the live `GET /api/mastery/summary` response.
- Live daily cost must be visible from real pi-harness cost snapshot data, not dry-run or `not_configured` placeholders.
- Section 0 frozen-repo and mock-mode criteria must be rechecked at strict milestone closure.
- `docs/AUDIT-MANUAL.md` is unrelated untracked workspace noise from the controller context and remains outside this note's modified set.

No code changes, dependency changes, or deletion operations are part of this documentation/evidence slice.
