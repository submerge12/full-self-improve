# M3 Review - Verification Phase 2

Date: 2026-06-14.

Status: Phase-2 implementation verified; strict milestone closure pending global Section 0 frozen-repo gate recheck or an explicit baseline for current external checkout state.

## Verified

- FSRS review queue exists and due concepts resurface on the due date.
  - Evidence: `src/engine/persistent-review.ts` defines review schedule upsert and due-list reads against the `reviews` table, requiring JSON-object FSRS state and ordering due reviews by due date and concept slug.
  - Evidence: `src/engine/persistent-review.test.ts` covers schedule upsert, due-date inclusion through the UTC next-day cutoff, exclusion of future reviews, generated/reviewed concept filtering, and corrupt/lossy FSRS-state rejection.
  - Checkpoints: `.ai/checkpoints/part-m3-fsrs-due-review/step-1.md` through `step-4.md`.
- Review attempt advances due date, updates FSRS state, updates mastery, and emits trace.
  - Evidence: `recordPersistentReviewAttempt` updates `reviews.fsrs_state` and `reviews.due_at`, records review-attempt trace data, updates mastery in the same transaction, and returns trace events.
  - Evidence: `src/engine/persistent-review.test.ts` verifies rating-specific intervals, review counts/lapses, mastery deltas, trace events, clamping, and no partial writes for invalid/corrupt inputs.
  - Checkpoints: `.ai/checkpoints/part-m3-review-attempt-update/step-1.md` and `step-2.md`.
- Application task is generated, attempted/graded, and moves mastery.
  - Evidence: `src/engine/persistent-application.ts` generates free-form application tasks from a concept page rubric, grades application attempts deterministically, writes attempts, updates mastery, and emits task/grade traces.
  - Evidence: `src/engine/persistent-application.test.ts` covers task creation, correct/partial/incorrect grading, persisted attempts, mastery movement, trace behavior, invalid input rollback, malformed rubric handling, and trace-failure rollback.
  - Checkpoints: `.ai/checkpoints/part-m3-application-task-engine/step-1.md` and `step-2.md`.
- Daily plan includes due review/application pairs before new learning.
  - Evidence: `src/engine/persistent-plan.ts` prepends `review` and `application` activities for due reviews before the generated learning queue, renumbers the full queue, and records a rationale noting due review/application pairs before new learning.
  - Evidence: `src/engine/persistent-plan.test.ts` verifies due review/application activities appear in order 1 and 2 before learn/quiz/teachback items, future reviews are excluded, stored plans are reused unless forced, and forced regeneration picks up newly due reviews.
  - Checkpoint: `.ai/checkpoints/part-m3-application-plan-queue/step-1.md`.
- CLI/API/App route surfaces expose the review/application flows for agents.
  - CLI evidence: `src/cli/kl.ts` includes `application` and `review` commands; `src/cli/kl.test.ts` covers application task creation/grading and review due-list/attempt flows with persisted traces and validation.
  - API evidence: `src/api/contracts.ts` includes `application.task.create`, `application.grade`, `review.due`, and `review.attempt` in the route manifest; `src/api/contracts.test.ts` verifies manifest identity and route matching.
  - Handler evidence: `src/api/handlers.ts` exposes pure handlers for application task, application grade, review due-list, and review attempt; `src/api/handlers.test.ts` covers envelopes, validation, trace persistence, and rollback/error classification.
  - App route evidence: `src/app/api/_shared/route-adapter.test.ts` verifies the actual Next route modules for authenticated application/review success paths and unauthenticated mutation rejection.
  - Checkpoints: `.ai/checkpoints/part-m3-application-cli/step-1.md`, `step-2.md`; `.ai/checkpoints/part-m3-application-api-core/step-1.md`, `step-2.md`; `.ai/checkpoints/part-m3-application-route-wrappers/step-1.md`; `.ai/checkpoints/part-m3-review-cli/step-1.md`, `step-2.md`; `.ai/checkpoints/part-m3-review-api-core/step-1.md`; `.ai/checkpoints/part-m3-review-api-routes/step-1.md`.

## Checklist Reconciliation

### PLAN Section 2.4 Phase 2 Rows

| Criterion | Status | Evidence |
| --- | --- | --- |
| FSRS spaced-repetition queue | Verified | Review schedule/due-list engine and tests; `part-m3-fsrs-due-review` checkpoints. |
| Due concept resurfaces on due date | Verified | Planner due-list cutoff and plan queue tests for `2026-06-14`; future-review exclusion test. |
| Review attempt updates FSRS state and mastery | Verified | `recordPersistentReviewAttempt` and focused review attempt tests; `part-m3-review-attempt-update` checkpoints. |
| Application tasks graded by execution/rubric | Verified for deterministic rubric grading | Application engine, CLI, API, and route-wrapper tests; application checkpoints. |
| Weakness-driven planning | Verified only for due review/application remediation queueing | Due review/application pairs are placed before new learning; existing mastery threshold planning remains in `persistent-plan` and is covered by plan tests. Full weakness-report-to-next-plan behavior is deferred unless direct evidence is added. |
| Trace events for Phase-2 pipeline steps | Verified | Review attempt, application task generation, application grading, plan, CLI, handler, and route-adapter tests assert trace behavior where the flow mutates state. |

### PLAN Section 5 M3 Row

| Done-when item | Status | Evidence |
| --- | --- | --- |
| A concept learned in M1 resurfaces on its FSRS due date | Verified at implementation/test level | Existing concept rows with review schedules resurface in `listDuePersistentReviews` and daily plan queue tests on the due target date. |
| An application task is generated | Verified at implementation/test level | Application engine, CLI, API handler, and route-wrapper tests create application tasks. |
| Application task is attempted and graded | Verified at implementation/test level | Application engine, CLI, API handler, and route-wrapper tests grade application responses. |
| Application grading moves mastery | Verified at implementation/test level | Application tests assert mastery score/confidence/attempt changes for correct, partial, and incorrect responses. |

### Closure-Gate Dependencies

- M1/M2 dependencies are not counted as M3 code failures in this note. They remain closure gates only where PLAN Section 5 requires every milestone to also satisfy covered checklists and Section 0 criteria.
- PLAN Section 5 also requires each milestone review note; this file satisfies the M3 review-note requirement for this documentation slice.
- Global Section 0 frozen-repo state was sampled by the controller on 2026-06-14 and is not clean/fully baselined: `G:\knowledge-showcase` returned `fatal: not a git repository`; `C:\Users\Holly\compass-health` and `G:\multica-ai-multica-https-github-com` had many tracked modifications and untracked files; `G:\pi-harness` had untracked `docs/context-cache-design.md` and `experiments/results/`; MathPilot was not successfully rechecked because the configured path contains mojibake/Chinese characters and needs an explicit path baseline.
- The external repo statuses above are current external checkout state only. This note does not claim those changes were caused by `knowledge-loop`.

## Verification

- Prior controller evidence: full `npm run check` passed on 2026-06-14 after `c446e9f Add M3 review API routes`; the successful escalated rerun followed a known sandbox `spawn EPERM`; result was 39 test files / 502 tests passed.
- Focused verification for this note:
  - `npm run test:unit -- src/engine/persistent-review.test.ts src/engine/persistent-application.test.ts src/engine/persistent-plan.test.ts src/api/handlers.test.ts src/app/api/_shared/route-adapter.test.ts src/api/contracts.test.ts`: passed, 6 test files / 114 tests.
  - `npm run typecheck`: passed.
  - `npm run lint`: passed.
  - `git diff --check -- docs/reviews/M3.md .ai/checkpoints/part-m3-review-note/step-1.md`: not sufficient by itself while both files are untracked, because `git diff --check` does not inspect untracked file content.
  - Content-level trailing whitespace check over `docs/reviews/M3.md` and `.ai/checkpoints/part-m3-review-note/step-1.md`: passed with no matches.

## Remaining Gates

- Strict M3 closure remains pending global Section 0 frozen-repo baseline/recheck. Because the controller sampling found dirty/not-git/incomplete external checkout state and MathPilot was not successfully rechecked, this note records that gate as pending, not passed.
- `docs/AUDIT-MANUAL.md` is unrelated untracked workspace noise and must remain unstaged and outside this note's modified set.
- No code changes, dependency changes, commits, staging, branches, pushes, or deletion operations were part of this documentation/evidence slice.
