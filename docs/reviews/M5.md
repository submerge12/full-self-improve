# M5 Review

## Status: deterministic M5 development evidence recorded

This note records deterministic M5 development evidence for the second adapter genericity proof, backup/restore drill, and read-only ops dashboard. It does not declare M4 complete, close earlier live gates, or certify the full system as live-ready.

## Genericity proof

- Second adapter: `GitRepoAdapter` is registered through adapter runtime config as a second `SourceAdapter` implementation.
- Deterministic tests: `npm run test:unit -- src/adapters/git-repo.test.ts src/adapters/config.test.ts` passed with 2 test files and 23 tests passing.
- Persistent ingest proof: `npx tsx .ai/tmp/part-m5-second-adapter-genericity-proof/run-proof.ts` passed outside the sandbox after sandbox `spawn EPERM`. Summary: `sourcesSeen: 4`, `sourcesProcessed: 4`, `sourcesSkipped: 0`, `sourcesFailed: 0`, `chunksCreated: 5`, `conceptsCreated: 4`, `pagesCreated: 4`. Table counts: `sources: 4`, `chunks: 5`, `concepts: 4`, `pages: 4`.
- Engine diff proof: `git diff -- src/engine` produced no output during the genericity checkpoint and again during Task4 verification.
- Final Task4 targeted proof: `npm run test:unit -- src/adapters/git-repo.test.ts src/adapters/config.test.ts src/db/backup.test.ts src/ops/dashboard.test.ts` passed with 4 test files and 33 tests passing.

## Backup and restore

- Deterministic evidence: `npm run test:unit -- src/db/backup.test.ts src/cli/kl.test.ts` passed on 2026-06-15 with 149 tests, covering backup manifest SHA-256 output, restore-drill `integrityOk: true`, missing/duplicate/unsafe CLI options, and source/destination same-path rejection.
- Runbook: `docs/runbooks/m5-backup-restore.md` records the scratch DB prep, backup creation, restore drill, and explicit-destination rerun rule.
- Operational CLI smoke: `npm run kl -- db-backup create --db .ai/tmp/m5/controller-m5-2-smoke-20260615-2203/knowledge-loop.db --out .ai/tmp/m5/controller-m5-2-smoke-20260615-2203/backups/knowledge-loop.backup.db` produced SHA-256 `9842acc35ebeb181cf6833aa365fb300223a12222a3a6c6a3e3dcc287ba6cba7` with `sources: 1`.
- Restore-drill smoke: `npm run kl -- db-backup restore-drill --backup .ai/tmp/m5/controller-m5-2-smoke-20260615-2203/backups/knowledge-loop.backup.db` returned `integrityOk: true` with the same SHA-256 hash.
- Broader check: `npm run check` passed after an escalated rerun, with 51 test files and 738 tests passing.

## Dashboards and polish

- Deterministic evidence: `npm run test:unit -- src/ops/dashboard.test.ts src/api/contracts.test.ts src/api/handlers.test.ts src/app/api/_shared/route-adapter.test.ts src/cli/kl.test.ts` passed on 2026-06-16 with 288 tests, covering the read-only domain summary, `GET /api/ops/dashboard` bearer route contract, authenticated/unauthenticated handler behavior, Next route wrapper runtime/export/auth, no-DB-creation unauthenticated rejection, existing-DB read-only route access, and `ops-dashboard --db` CLI JSON shape and usage errors.
- API route: `GET /api/ops/dashboard` is documented as bearer-authenticated and returns `{ summary }` with routeId `ops.dashboard`. The actual Next route pre-authorizes requests before creating a runtime DB context and opens the runtime database read-only with `fileMustExist`.
- CLI runbook: `docs/runbooks/m5-ops-dashboard.md` records `npm run kl -- ops-dashboard --db .ai/tmp/m5/knowledge-loop.db`.
- CLI smoke: `npm run kl -- ops-dashboard --db .ai/tmp/m5/controller-m5-3-dashboard-smoke-20260615-2240/knowledge-loop.db` returned `sources: 1`, `chunks: 1`, `trace_events: 1`, and `recentTraceEventCount: 1`.
- Broader check: `npm run check` passed after an escalated rerun, with 52 test files and 748 tests passing.
- Final Task4 API/CLI/route proof: `npm run test:unit -- src/api/contracts.test.ts src/api/handlers.test.ts src/app/api/_shared/route-adapter.test.ts src/cli/kl.test.ts` passed with 4 test files and 285 tests passing.

## Earlier live gates not closed by M5

- M2 live Multica/scheduler proof remains outside this note until recorded in `docs/reviews/M2.md`.
- M4 live Windows logger, Coach publish, and one-week compass-health hash proof remain outside this note until recorded in `docs/reviews/M4.md`.
- M5 evidence does not close earlier M1/M3 Section 0 strict closure checks; those remain closure-time verification items.
